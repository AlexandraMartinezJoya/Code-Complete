//compute roots of a quadratic equation
//This code assumes that (b*b - 4* a * c) is positive

temp = Sqrt(b*b - 4 * a * c);
root[0] = (-b + temp) / (2 * a);
root[1] = (-b - temp) / (2 * a);
...

//swap the roots
temp = root[0];
root[0] = root[1];
root[1] = temp;
